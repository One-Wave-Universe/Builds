# Status

## Proposed cell + algorithm. Not measured hardware.

Locked in writing:

- Weight = physical lean off a moving ground (`cell-v1/WEIGHT_LEAN.md`)
- Not a software parameter file (`cell-v1/NOT_SOFTWARE.md`)
- Algorithm may only commit a lean at T6 (`algorithms/LEAN_INTO_ZER0.md`)

Not locked by measurement:

- `cell-v1/LOG.md` is empty → hardware score 0%
- No identical-probe / different-prior-state result yet

`algorithms/lean_weight.py` is a sandbox receipt. It is not a weight dump.
