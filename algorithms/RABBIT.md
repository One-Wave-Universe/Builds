# Rabbit Hopping — algorithm card

Reversible packet: `(source_rank N, center T, wrapper W)`

Source identity stays fixed. Generated center moves. Wrappers are opposite-parity neighbors. Route family and polarity are kept.

```bash
curl -O https://raw.githubusercontent.com/One-Wave-Universe/RABBIT-HOPPING/main/rabbit_hopping.py
python3 rabbit_hopping.py
```

Repo: https://github.com/One-Wave-Universe/RABBIT-HOPPING

Arithmetic is the artifact. Nested Point/Path/Field physics is tagged hypothesis, not a grant claim.
