# Do not translate this into the standard system

Forbidden substitutions:

- lean → weight file / parameter / float / coefficient
- hardness → learning rate / regularization
- use → training step / backprop update
- HOLD → off / zero / disabled / parked
- floating ground → stored bias term
- memory → log / receipt / checkpoint / saved state / q in a .py file
- T6 → commit() in software as if the body had moved
- style → downloaded personality weights
- cell → neuron as a multiply-accumulate

If a sentence still works after you swap in those standard words, the sentence is wrong for this repo.

The thing is the lean in the path. Measure it or it is not there.
