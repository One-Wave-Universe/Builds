# Both ways

**Both readings are on.**

- Three axes A B C — each bidirectional and spatial. A/B/C are not voltage levels.
- Each axis is an opposed +/− differential loop around the nucleus.
- The live state can sit near balance or lean either direction without redefining the axis.
- Three voltage-gated depths CHOICE → PIVOT → FLIP — each bidirectional in the sense that the state may promote or relax through the bands.
- Last action down = FLIP, one pair reversed. Not extra invert hardware.

## Voltage-gated depth

The ladder is:

```
HOLD / home wobble
      ↕  threshold + hysteresis
CHOICE
      ↕  threshold + hysteresis
PIVOT
      ↕  threshold + hysteresis
FLIP
```

This ladder is driven by differential magnitude relative to CENTER plus retained-state bias and bus / neighbor condition. It is not A → B → C.

POINT → PATH → FIELD is a separate scale relation produced by coupled cells / states; do not use those names as transistor-gate steps.

Same silicon both directions. Body diodes cancelled (back-to-back) or isolated-body later.

Never both directions ON at once in a hard-conduction sense. GAP is the transition / inhibit region. A live opposed differential may still have both sides biased on around CENTER; "not both ON" here means do not command a destructive shoot-through path.

Views up on the 8. Collapse still to V_BUS.
